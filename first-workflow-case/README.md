# 白塔疑案

这是一个小说大纲工作区。

当前它承担仓库里的“第一个完整 workflow 案例”：

- novel: `白塔疑案`
- protagonist: `林舟`
- current case: `idea-20260522-001`
- current stage: `check-consistency` 已通过，`plan-merge` 已生成，尚未 `apply`
- case note: `reports/first-workflow-case.md`

建议流程：

1. 先把碎片想法录入 `inbox/`
2. 再把确认后的内容并入 `canon/`、`outline/`、`timeline/`
3. 每轮改动后运行 validator
4. 看 `reports/` 和 `state/workspace-status.json`

当前目录结构：

```text
.
├── canon/
├── constraints/
├── inbox/
├── outline/
├── reports/
├── state/
├── timeline/
└── views/
```
