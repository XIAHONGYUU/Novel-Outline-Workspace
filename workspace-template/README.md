# {{NOVEL_NAME}}

这是一个小说大纲工作区。

建议流程：

1. 先把碎片想法录入 `inbox/`
2. 再把确认后的内容并入 `canon/`、`outline/`、`timeline/`
3. 每轮改动后运行 validator
4. 看 `reports/` 和 `state/workspace-status.json`

当前目录结构：

```text
.
├── canon/
├── constraints/
├── inbox/
├── outline/
├── reports/
├── state/
├── timeline/
└── views/
```
